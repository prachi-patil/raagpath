# Copy this to .env on EC2 and fill in real values
# NEVER commit .env to git

SPRING_DATASOURCE_URL=jdbc:postgresql://<RDS-ENDPOINT>:5432/raagpath
DB_USERNAME=raagpath
DB_PASSWORD=CHANGE_ME

# Generate with: openssl rand -hex 32
JWT_SECRET=CHANGE_ME_64_CHAR_HEX_STRING
